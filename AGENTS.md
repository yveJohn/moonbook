# Moonbook 重构仓库协作规则

## 1. 权威文档

开发前必须完整阅读并遵守：

1. `docs/superpowers/specs/2026-08-14-moonbook-gin-vue-admin-refactor-design.md`
2. `docs/goal-mode/2026-08-14-moonbook-refactor-goal.md`

若实现、计划或临时决定与已批准设计冲突，必须以设计文档为准。确需改变已批准边界时，先记录冲突、影响和候选方案并等待用户确认，不得静默改变需求。

## 2. 项目边界

- 新项目目录固定为 `/Users/yve/code/ai-project/moonbook`。
- 旧项目 `/Users/yve/Documents/moonbook` 是冻结的迁移来源和兼容基线，默认只读。
- 不得修改、删除、重置或提交旧项目中的用户修改。
- 管理后台后端和管理前端基于固定 Gin-Vue-Admin `main` commit 重构。
- `reader-ui` 必须从旧项目原样迁入，切换验收前不得进行业务重构。
- 读者兼容范围以冻结版 `reader-ui` 实际调用的接口为准，必须实现零代码修改切换。

## 3. 技术约束

- 后端：Go、Gin-Vue-Admin、模块化单体。
- 管理前端：Gin-Vue-Admin 自带 Vue 技术栈和既有工程约定。
- 结构化数据：PostgreSQL。
- 缓存、限流、短期会话和任务协调：Redis。
- 章节正文、封面、导入导出文件和附件：MinIO。
- 首期部署：Docker Compose；设计必须保持后续迁移 k3s 的可移植性。
- 不引入微服务、CDC、双写或旧 MySQL 表结构兼容层。

旧业务 ID 必须原值迁移为 PostgreSQL `bigint`。所有面向 JavaScript 的 Long/雪花 ID，包括但不限于 `id`、`bookId`、`chapterId`、`sourceId`、`boardId`、`importTaskId` 和 `targetBookId`，必须作为 JSON 字符串输出，并在管理前端按字符串处理和提交。禁止使用 `Number`、`parseInt`、`el-input-number` 或算术方式处理这些 ID。

## 4. Gin-Vue-Admin 基线规则

- 首次导入源码时，从官方仓库取得当时的 `main`。
- 导入后立即把远端 URL、branch 和完整 commit hash 写入版本锁定文档。
- 必须保留本仓库现有设计提交的 Git 历史，不得用覆盖、重置或重新初始化仓库的方式导入上游。
- 固定 commit 后不自动跟随上游；后续安全修复只能按明确需求选择性移植。
- 业务能力放入边界清晰的独立模块，尽量不侵入 GVA 公共基座。

## 5. 本地基础设施授权

允许直接使用本机 Docker 启动、停止和重建本项目专用的 PostgreSQL、Redis 与 MinIO 容器，用于开发、迁移演练和自动化测试。必须遵守：

- 使用本项目 Compose 文件、明确的容器名和独立命名卷；
- 不复用或删除其他项目的容器、网络或卷；
- 销毁数据卷前确认其只属于本项目，并优先使用可恢复方式；
- 默认凭据只用于本地开发，生产凭据不得写入仓库；
- 测试需要清空数据时只操作明确标识的测试数据库、测试 Bucket 和测试 Redis DB/前缀；
- 任务结束时不必停止开发基础设施，除非它影响验证、占用冲突端口或用户明确要求。

不得自行连接、修改或迁移生产数据库、生产 MinIO、生产 Redis、生产 DNS、生产反向代理或真实支付环境。正式停机迁移和流量切换需要用户单独明确授权。

## 6. 数据库与迁移规范

- PostgreSQL 结构变更只能通过仓库选定的版本化迁移工具新增迁移文件。
- 已提交或已执行的版本迁移不得修改、改名或删除；修复必须新增更高版本。
- 迁移必须支持空库从零构建，并在真实 PostgreSQL 容器验证。
- 业务迁移程序只读访问旧 MySQL，按主键范围或游标分批读取。
- 迁移程序必须支持检查点、中断恢复、幂等重跑、错误清单和自动校验报告。
- 禁止使用无条件 `DELETE`、`TRUNCATE`、`DROP DATABASE` 或其他破坏性数据操作，除非用户明确授权且已有备份和验证方案。
- 财务数据迁移必须做到余额、流水、订单、支付记录和权益零差异。
- MinIO 对象写入后必须校验大小和 SHA-256；PostgreSQL 不得引用尚未成功写入的对象。

## 7. 安全规则

- 密码、Token、私钥、支付凭据、数据库密码和第三方密钥只能通过环境变量或 Secret 注入。
- 仓库只提供 `.env.example` 等无敏感值模板。
- 日志、测试快照和迁移报告不得包含完整 Token、密码、支付密钥或完整敏感正文。
- 第三方服务必须有启用开关、超时、有限重试、幂等策略和结构化失败记录。
- 管理员身份体系与读者身份体系必须隔离。

## 8. 实施和验证规则

- 按批准设计中的纵向阶段实施，不做与当前阶段无关的大范围重构。
- 每个业务域必须同时完成后端、管理页面、数据模型、旧数据映射和自动化测试。
- 集成测试使用真实 PostgreSQL、Redis 和 MinIO，不用 SQLite 或本地文件替代生产组件。
- 读者接口使用独立路由、DTO、响应封装和契约测试，GVA 管理响应格式不得泄漏。
- 修改代码后必须执行与风险相称的格式检查、静态检查、单元测试、集成测试和构建。
- 可以为本地集成和 E2E 验证启动项目应用进程；验证完成后应停止临时进程。不得启动或重启生产应用。
- 不得通过删除、跳过或弱化测试来换取通过。
- 发现旧系统现存缺陷时，先判断冻结版 `reader-ui` 是否依赖该行为；兼容与安全冲突必须记录并请求用户决策。

## 9. Git 提交规则

每完成一个逻辑清晰、已验证的功能单元后必须提交，不要把整个长期目标堆积成一个提交。

提交前必须：

1. 检查全部已跟踪和未跟踪修改；
2. 排除构建产物、日志、缓存、IDE/系统文件和敏感信息；
3. 检查暂存区内容和 `git diff --cached --check`；
4. 按功能拆分提交，不能遗漏其他真实有效的项目修改；
5. 使用简体中文、简短明确的提交信息；
6. 提交后执行 `git status`。

不得自行执行 `git push`、强制推送、变基、重置、删除用户修改或改写历史。遇到无法判断用途、可能敏感或相互冲突的文件时，保留文件并在状态报告中说明。

## 10. 目标模式持续执行规则

- 以目标文档的完成判据为终点，不以“已搭好骨架”“主要功能完成”或“测试基本通过”为完成。
- 每轮开始先检查当前 Git、里程碑状态、失败测试和剩余完成判据，从上次进度继续，不重复已完成工作。
- 维护 `docs/progress/refactor-status.md`，逐项记录已完成、验证证据、当前工作、已知问题和下一步。
- 安全且在范围内的实现决策自行作出，不因普通技术困难暂停。
- 外部服务没有真实凭据时，完成适配器、配置、契约测试和本地替身验证，并明确记录仍需用户执行的真实环境验收。
- 只有在需要生产权限、不可推断的产品选择、真实第三方凭据或有风险的破坏性操作时才请求用户输入。
- 不得把缺少生产权限当作提前结束整个目标的理由；继续完成所有不依赖该权限的工作。
- 未完成目标时不要宣称完成，也不要把生产切换就绪误称为已经完成生产切换。

## 11. 服务影响说明

每次向用户汇报代码修改时，必须说明影响了哪些服务，以及本地或部署环境中哪些服务需要重启。仅文档变更应明确说明“不影响服务，无需重启”。
