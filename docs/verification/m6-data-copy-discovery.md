# M6 完整数据副本候选定位记录

检查日期：2026-08-21

## 结论

冻结旧仓库中已定位到一个与用户描述的“约 8 GB 数据库备份”相符的候选文件：

`exports/moonbook_admin_20260715_175649.sql.gz`

候选已完成全流 SHA-256 和 `gzip -t` 校验，但尚需以隔离 MySQL 的真实恢复结果证明 SQL 可恢复及实际解压字节数。候选存在且 gzip 完整不等于 M6 演练已经完成。

## 元数据

| 项目 | 结果 |
| --- | --- |
| 冻结仓库 | `/Users/yve/Documents/moonbook` |
| 相对路径 | `exports/moonbook_admin_20260715_175649.sql.gz` |
| 压缩大小 | 1,065,163,488 字节，约 1.0 GiB |
| 文件系统修改时间 | 2026-07-15 17:58:21 +08 |
| 文件类型 | gzip compressed data，来源 Unix |
| gzip 内部修改时间 | 2026-07-15 09:57:13 UTC |
| gzip ISIZE | 3,834,607,260 字节，按 gzip 规范为原始大小模 `2^32` |
| SHA-256 | `94d86df76780cc85cdec4690265866140b202bb2b5d908826bf37783c9c8cbe3` |
| gzip 全流校验 | `gzip -t` 通过 |
| 旧仓库 Git 状态 | 未跟踪，且没有 `.gitignore` 规则命中 |

演练前曾结合“约 8 GB”描述假设 ISIZE 绕回一次：

```text
3,834,607,260 + 4,294,967,296 = 8,129,574,556 bytes
```

隔离恢复的流式计数现已证明实际 SQL 为 3,834,607,260 字节，等于 ISIZE，没有发生 `2^32` 绕回；上述 8,129,574,556 字节假设已被否定。恢复被 MySQL 完整接受，耗时 64 秒，约 59.92 MB/s（57.14 MiB/s）。

## 安全边界

- 文件留在冻结旧仓库，未复制到新仓库，也未修改旧仓库或其 `.gitignore`；
- 文件当前是未跟踪内容，禁止把它加入任一 Git 提交；
- 后续命令不得把 SQL、密码摘要、Token、支付凭据或完整正文输出到终端、日志或报告；
- 只能恢复到明确的本地隔离 MySQL 项目和命名卷，源文件只读挂载；
- 不连接生产 MySQL、PostgreSQL、Redis、MinIO 或第三方服务；
- 在内容生产 stage、全域核对器和对象迁移尚未完成前，不把部分导入结果描述为完整副本演练。

## 容量初检

旧仓库、新仓库和 `/private/tmp` 位于同一数据卷。检查时文件系统约 1.8 TiB，总可用约 985 GiB；宿主磁盘空间不是当前直接阻断项。

随后通过现有 `moonbook-postgres-1` 和 `moonbook-minio-1` 容器只读检查数据目录挂载，二者均位于 `/dev/vdb1`，文件系统总容量为 1,080,519,819,264 字节，当前可用 1,003,607,867,392 字节，约 934.68 GiB。`docker system df` 同时记录到镜像 26.07 GB、容器可写层 2.515 GB、命名卷 16 GB 和构建缓存 39.23 GB；本次没有回收或修改任何其他项目资源。

该结果确认当前 Docker 数据文件系统容量不是直接阻断项，但不能证明完整演练的 MySQL/PostgreSQL 数据膨胀、MinIO 对象容量和峰值临时空间一定充足。正式演练仍须采集开始、峰值和结束水位，并保留安全余量。

## 进入完整演练前的门槛

1. 内容生产迁移 stage、legacy 唯一键、对象转换和全域核对器已完成；
2. gzip 完整性和固定 SHA-256 已完成，运行时必须再次校验同一值；
3. 为隔离 MySQL、PostgreSQL、Redis 和 MinIO 分配唯一 Compose 项目、端口、网络及命名卷；
4. 只读挂载候选文件，恢复前记录 Docker 可用磁盘，设置日志脱敏和受控临时目录；
5. 先识别 dump 工具版本、字符集、数据库名和恢复参数，不执行生产连接；
6. 完整运行盘点、`moonbook-legacy-migrate all`、财务/对象/关系核对和幂等重跑；
7. 记录各 stage 行数、错误、吞吐、耗时、峰值资源及 12 小时窗口余量；
8. 演练结束后只清理明确的 `moonbook_verify_` 隔离资源，不删除候选副本。

当前候选已经关闭路径、压缩流完整性、内容指纹、真实 SQL 字节数和 MySQL 可恢复性缺口。源库存在 16 个 TXT 任务但原文件未被旧实现持久化且本机未定位到匹配文件，完整迁移在该 No-Go 门停止；全域迁移/核对/重跑、峰值资源和 12 小时窗口仍待原文件恢复后继续。
